import socket
import sys

# Create socket using the following

def socket_create():
        try:
            global host
            global port
            global socket
            host = ''
            port = 9999            